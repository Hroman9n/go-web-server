---
name: GitHub Actions
run-name: "${{ github.actor }} testing actions on ${{ github.run_id }} workflow"
on:
  - push
jobs:
  Test-actions:
    runs-on: self-hosted
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - name: Build image
        run: |
          docker build -t "hub.docker.com/hroman9n/go-web-server:0.0.1" .
      - name: Publish image to Dockerhub
        shell: bash
        env:
          DOCKERHUB_LOGIN: ${{ secrets.DOCKERHUB_LOGIN }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          docker login -u "${DOCKERHUB_LOGIN}" -p "${DOCKERHUB_TOKEN}" && \
          docker push hub.docker.com/hroman9n/go-web-server:0.0.1
      - run: echo "🍏 This job's status is ${{ job.status }}."
